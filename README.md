# Learning TensorFlow

TensorFlow is an open source library. It is used for numerical computations, in data flow graphs.

The graphs have nodes and edges. Nodes = mathematical operations; edges = data (ususlly tensors).

